import { NavigationConfig } from '../types/navigation';
import navigationData from '../../MockDirectory/navigation/navigation.json';

export const navigationConfig: NavigationConfig = navigationData as NavigationConfig;
